{% extends "base.html.twig" %}

{% block title %}
	produit
	{{ tarticle.libCrtFr }}
{% endblock %}
{% block body %}
	{# {{ dump(app.flashes('success'))}} #}

	<ol class="breadcrumb">
		<li class="breadcrumb-item">
			<a href="{{ path('homepage') }}">Accueil</a>
		</li>
		<li class="breadcrumb-item">
			<a href="{{ path('product_category',{'cod': tarticle.clrCtg.cod } ) }}">{{tarticle.clrCtg.dsg}}</a>
		</li>

		<li class="breadcrumb-item active">{{product.lblCrtFr}}</li>
	</ol>
	<h1>
		Produit :
		{{ tarticle.cod }}
		({{tarticle.libCrtFr}}
		€)
		<div
			class="row">
			{# <div class="col-4">
																			<img class="img-fluid" src="{{ product.mainPicture}}" alt="image de {{ product.name }}">
																		</div> #}

			<div class="col">
				<p>{{ product.shortDesciption }}</p>
				<a href="{{ path('cart_add',{'id': tarticle.id} ) }}" class="btn btn-success btn-lg">
					<i class="fas fa-shopping-cart"></i>
					Ajouter à la grille tarifaire
					<a/>
				</div>

			</h1>


		{% endblock %}
